defaults:
  - /probing/no_layout
  - /probe/quick
  - sampling: perplexity_pruned
  - _self_

dataset: lczerolens/tcec-boards
n_roots_train: 8
n_roots_test: 4
height: 2
max_paths_per_root: 16
max_nodes_total: 500
branching: perplexity
fixed_k: null
seed: 42
probe:
  probe_epochs: 2
  probe_batch_size: 8
temp_base: 1.0
temp_end: 2.0
