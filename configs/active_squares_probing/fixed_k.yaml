defaults:
  - /probing/no_layout
  - /probe/small_batch
  - sampling: random
  - _self_

dataset: lczerolens/tcec-boards
n_roots_train: 16
n_roots_test: 8
height: 2
max_paths_per_root: 24
max_nodes_total: 2000
branching: fixed_k
fixed_k: 4
seed: 42
probe:
  probe_epochs: 5
  probe_batch_size: 32
temp_base: 1.0
temp_end: 2.0
