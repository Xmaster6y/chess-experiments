defaults:
  - optional puzzle_stats: null
  - optional mate_in_1_probing: null
  - optional mate_in_3_probing: null
  - optional cross_model_probing: null
  - optional piece_pruning: null
  - _self_

hydra:
  run:
    dir: results/hydra/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
  sweep:
    dir: results/hydra/${hydra.job.name}_sweep/${now:%Y-%m-%d_%H-%M-%S}

wandb:
  project: sweep-test
  entity: yp-edu
  mode: offline
